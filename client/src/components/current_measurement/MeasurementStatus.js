// Shows the measurement status, red, yellow, green
import React, {Component} from 'react';
import { reduxForm } from 'redux-form';

class MeasurementStatus extends Component {
  render() {
    return (
      <div>
        poopydoop
      </div>
    );
    /*
    switch (status) {
      case 'on':
        return (
          <div>
            GSF status: {this.props.status}
          </div>
        );
        break;
      case 'error':
        return (
          <div>
            GSF status: {this.props.status}
          </div>
        );
        break;
      default:
        return (
          <div>
            GSF status: {this.props.status}
          </div>
        );
        break;
    */
  }
};

export default MeasurementStatus;
